@*<p>onHoverRatingChangeResult: {{onHoverRatingChangeResult | json}}</p>
<p>onClickResult: {{onClickResult | json}}</p>
<p>onRatingChangeResult: {{onRatingChangeResult | json}}</p>*@

<div *ngIf="searchQueryParams">
    <h2 >No results for...</h2>
</div>

<div infinite-scroll
     [infiniteScrollDisabled]='infinitydisable'
         [infiniteScrollDistance]="2"
         [infiniteScrollThrottle]="300"
         (scrolled)="onScroll()">
    <div *ngIf="instructions">
        <h2 *ngIf="instructions.length==0 && !searchQueryParams">No instruction...</h2>
    </div>
    <div *ngFor="let instruction of instructions">
        <div class="panel panel-default">
            <div class="panel-header">
                <div class="row">
                    <span class="name col-md-8 col-lg-9 col-sm-7 col-xs-6">{{instruction.name}}  <small class="text-muted"> {{instruction.category.name}}</small></span>
                    <div class="rating col-md-4 col-lg-3 col-sm-5 col-xs-6">
                        <star-rating-comp class="hidden-xs" [starType]="'svg'"
                                          [step]="0.5"
                                          [speed]="'slow'"
                                          [size]="'large'"
                                          [rating]="setrating(instruction.ratingRelation)"
                                          [staticColor]="'default'"
                                          [hoverEnabled]="true"
                                          (onClick)="onClick($event)"
                                          (onRatingChange)="onRatingChange($event)"
                                          (onHoverRatingChange)="onHoverRatingChange($event)">
                        </star-rating-comp>
                        <star-rating-comp class="visible-xs" [starType]="'svg'"
                                          [step]="0.5"
                                          [speed]="'slow'"
                                          [staticColor]="'default'"
                                          [hoverEnabled]="true"
                                          [rating]="setrating(instruction.ratingRelation)"
                                          (onClick)="onClick($event)"
                                          (onRatingChange)="onRatingChange($event)"
                                          (onHoverRatingChange)="onHoverRatingChange($event)">
                        </star-rating-comp>

                    </div>
                </div>
                <div class="tag" style="display:inline;"><span class="glyphicon glyphicon-cloud"></span><small class="text-muted " *ngFor="let tag of instruction.tagsRelation"> #{{tag.tag.name}}</small></div>
            </div>
            <div class="panel-body">
                <img class="img-responsive" src="http://minionomaniya.ru/wp-content/uploads/2015/10/%D0%BA%D0%B0%D1%80%D1%82%D0%B8%D0%BD%D0%BA%D0%B8-%D0%B8%D0%B7-%D0%BC%D1%83%D0%BB%D1%8C%D1%82%D1%84%D0%B8%D0%BB%D1%8C%D0%BC%D0%B0-%D0%BC%D0%B8%D0%BD%D1%8C%D0%BE%D0%BD%D1%8B-2015.jpg">
                <div class="paneltextbody">
                    <p class="lead">This is a wider card with supporting text below as a natural lead-in to additional content. This content is a little bit longer.This is a wider card with supporting text below as a natural lead-in to additional content. This content is a little bit longer.</p>
                    <p><small class="text-muted ">{{ 'Create date' | translate:lang }}: {{instruction.createDate | date:'MM/dd/yyyy'}}  Author:<span class="profile-link" [routerLink]="['../../profile']" [queryParams]="{'user':instruction.author.id}"> {{instruction.author.fullName}}</span></small></p>
                </div>
            </div>
            <div class="panel-footer">
                <small class="text-muted text-left">{{ 'Last updated' | translate:lang }} {{instruction.lastChangedDate  | date:'MM/dd/yyyy'}}</small>
                <a class="btn btn-default right">{{ 'Read more' | translate:lang }}</a>
            </div>
        </div>
    </div>
</div>